name: qanswer
description: "A new Flutter project for education CRUD system."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # SQLite ডাটাবেস ব্যবহারের জন্য এই দুটি প্যাকেজ যোগ করা হয়েছে
  sqflite: ^2.3.0
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
